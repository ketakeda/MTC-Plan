# クエスト種別 テーブル出力定義

inputFile						# 入力ファイル
	${MTC-Developer}\Master\1.8.0\current\data\MTC リスト DBMaster.xlsx

Sheet							# シート名
	ミックスクエスト

Row								# 行範囲
	5-

ValidationCheck					# データを有効／無効をチェックするカラム
	$B

Format							# 出力フォーマット
	id,$B; schedule_id,$C
	title_type,$D; title_id,$E; group_id,$F
	display_type,$G; icon_id,$H; starting_flag,$I
	action_type1,$J; action_id1,$K; count_type1,$L; condition_count1,$M
	action_type2,$N; action_id2,$O; count_type2,$P; condition_count2,$Q
	action_type3,$R; action_id3,$S; count_type3,$T; condition_count3,$U
	required_quests,$V; required_act_type,$W; required_act_id,$X; required_cond_cnt,$Y
	random_id,$Z; cooldown,$AA; period_reset_type,$AB
	possible_period_count,$AC; possible_clear_count,$AD; reward_deal_id,$AE

UTF8BOM							# UTF-8 BOM 有無設定
	false

# エリアーコードデータ csv出力定義
outputFile						# 出力ファイル
	csv/mix_quests.csv

Conv
