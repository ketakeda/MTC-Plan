#

inputFile						# 入力ファイル
	${MTC-Developer}\plan\data\MTC リスト DBMaster.xlsx

Sheet							# シート名
	課金ショップ

Row								# 行範囲
	4-

HeaderDefRow					# ヘッダー判定に使用する行
	3

ValidationCheck					# データを有効／無効をチェックするカラム
	$B

Format							# 出力フォーマット
	id,$B; name,$E; type,$C; price,$F; stone_amount,$G
	yd_chip_id,$K
	reward_deal_id,$L; order,$M; android_product_id,$N; ios_product_id,$O
	limit_type,$P; limit_time,$Q; banner,$R
	telop_type,$D; schedule_id,$H

# エリアーコードデータ csv出力定義
outputFile						# 出力ファイル
	csv/charge_shops.csv

Conv

