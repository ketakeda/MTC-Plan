# MEMO: スケジュールに対応した変更が必要
# クエスト種別 テーブル出力定義

inputFile						# 入力ファイル
	${MTC-Developer}\plan\data\MTC リスト DBMaster.xlsx

Sheet							# シート名
	クエスト種別

Row								# 行範囲
	5-

ValidationCheck					# データを有効／無効をチェックするカラム
	$C

Format							# 出力フォーマット
	id,$B; type,$C; title,$D
	schedule_id,$E

UTF8BOM							# UTF-8 BOM 有無設定
	false

# エリアーコードデータ csv出力定義
outputFile						# 出力ファイル
	csv/quests.csv

Conv