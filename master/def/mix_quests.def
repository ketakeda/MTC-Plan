# クエスト種別 テーブル出力定義

inputFile						# 入力ファイル
	${MTC-Developer}\Master\2.0.0\currentcurrent\data\MTC リスト DBMaster.xlsx

Sheet							# シート名
	ミックスクエスト

Row								# 行範囲
	5-

ValidationCheck					# データを有効／無効をチェックするカラム
	$B

Format							# 出力フォーマット
	id,$B; schedule_id,$C
	title_type,$D; title_id,$E; group_id,$F
	accumulated_group,$G
	display_type,$H; icon_id,$I; starting_flag,$J
	action_type1,$K; action_id1,$L; count_type1,$M; condition_count1,$N
	action_type2,$O; action_id2,$P; count_type2,$Q; condition_count2,$R
	action_type3,$S; action_id3,$T; count_type3,$U; condition_count3,$V
	required_quests,$W; required_act_type,$X; required_act_id,$Y; required_cond_cnt,$Z
	random_id,$AA; cooldown,$AB; period_reset_type,$AC
	possible_period_count,$AD; possible_clear_count,$AE; reward_deal_id,$AF

UTF8BOM							# UTF-8 BOM 有無設定
	false

# エリアーコードデータ csv出力定義
outputFile						# 出力ファイル
	csv/mix_quests.csv

Conv
